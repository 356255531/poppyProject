in vrep and poppy main, there are additional,
but optional plotting methods. So, don't get confused
by unused variables.

Overall, the TD Policy Iteration has its own epsilon greedy
policy, so the epsilon of the actor is set to zero.